{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Trỏ tới PostgreSQL trong Docker
    "DefaultConnection": "Host=localhost;Port=5432;Database=hms_db;Username=postgres;Password=hms_password_123",
    // Trỏ tới Redis trong Docker
    "RedisConnection": "localhost:6379"
  }
}
